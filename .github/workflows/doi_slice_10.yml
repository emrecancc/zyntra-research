name: DOI Collector - Slice 10

on:
  schedule:
    - cron: "32 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 28
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main
          fetch-depth: 1

      - name: Onceki ozeti getir (summary icin)
        run: |
          git fetch origin slice/10 --depth=1 2>/dev/null || true
          git checkout origin/slice/10 -- data/slice_10/summary.json 2>/dev/null || true
          mkdir -p data/slice_10

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -q httpx

      - name: DOI Collector Slice 10
        run: timeout 1380 python scripts/doi_collector_slice.py --job-id 10 --total-jobs 20 || true

      - name: Push slice/10
        run: |
          git config user.name "zyntra-bot"
          git config user.email "bot@zyntra.ai"
          git add data/slice_10/summary.json data/slice_10/progress.json 2>/dev/null || true
          # JSONL cok buyuyor - sadece summary/progress push et, JSONL'u gitignore et
          echo "data/slice_10/dois.jsonl" >> .gitignore
          git add .gitignore
          git diff --staged --quiet && exit 0
          TOTAL=$(python3 -c "import json,pathlib; f=pathlib.Path('data/slice_10/summary.json'); print(json.loads(f.read_text()).get('new_this_run',0) if f.exists() else 0)" 2>/dev/null || echo 0)
          git commit -m "$(date +%H%M) +${TOTAL}DOI slice10"
          git push --force origin HEAD:refs/heads/slice/10