name: DOI Collector - Slice 18

on:
  schedule:
    - cron: "56 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 28
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Bagimliliklari kur
        run: pip install -q httpx

      - name: DOI Collector Slice 18
        run: timeout 1380 python scripts/doi_collector_slice.py --job-id 18 --total-jobs 20 || true

      - name: Push to slice branch
        run: |
          git config user.name "zyntra-bot"
          git config user.email "bot@zyntra.ai"
          BRANCH="slice/18"
          git checkout -B "$BRANCH"
          git add data/slice_18/ || true
          if git diff --staged --quiet; then
            echo "no change"
          else
            TOTAL=$(python3 -c "import json,pathlib; f=pathlib.Path('data/slice_18/summary.json'); print(json.loads(f.read_text()).get('new_this_run',0) if f.exists() else 0)" 2>/dev/null || echo "?")
            git commit -m "slice/18 $(date +%H%M) +${TOTAL}DOI"
            git push origin "$BRANCH" --force
          fi